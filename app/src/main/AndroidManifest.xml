<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.papierflieger" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_blue"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_blue"
        android:supportsRtl="true"
        android:theme="@style/Theme.Papierflieger"
        tools:targetApi="31" >
        <activity
            android:name=".presentation.ui.admin.AdminActivity"
            android:theme="@style/Theme.Papierflieger.ActionBar"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.home.profiles.settings.PasswordSecurityActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.home.profiles.settings.AccountInformationActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.auth.AuthActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.SplashScreenActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>